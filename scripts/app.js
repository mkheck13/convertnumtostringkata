// We need a function that can transform a number (integer) into a string.


function numberToString(num) {
    // Return a string of the number here!
  }

// I remembered that there was method to convert a number to a string
// So I just returned num with the .toString method
  function numberToString(num) {
    return num.toString();
  }